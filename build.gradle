task packageDistribution(type: Zip){
    from ('src') {into '.'}
    archiveFileName = "test.zip"
    destinationDirectory = file("build")
}


task test(type:Exec) {
  //println 'Executing tests'
  executable 'sh'
  args '-c', "python3 -m pytest \
    'src/test.py'"
}
